[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    requires_gcc: needs gcc + gdb on PATH (skipped otherwise)
    requires_gpp: needs g++ + gdb on PATH (skipped otherwise)
    requires_jdk: needs javac + java on PATH (skipped otherwise)
